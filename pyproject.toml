[project]
name = "ecmp-manager"
version = "1.0.0"
description = "Multi-WAN ECMP and failover for FRRouting"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "scapy==2.6.1",
    "toml==0.10.2",
]

---
name: Test ecmp-manager

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: Test ecmp-manager
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        container: [ecmp-manager]
    steps:
      # Checkout repository
      # https://github.com/actions/checkout
      - name: Checkout repository
        uses: actions/checkout@v4.2.2
      # Install and start FRR
      - env:
          FRRVER: frr-9.1
        name: Install and start FRR
        run: |
          curl -s https://deb.frrouting.org/frr/keys.gpg | sudo tee /usr/share/keyrings/frrouting.gpg > /dev/null
          echo deb '[signed-by=/usr/share/keyrings/frrouting.gpg]' https://deb.frrouting.org/frr \
            $(lsb_release -s -c) $FRRVER | sudo tee -a /etc/apt/sources.list.d/frr.list
          sudo apt-get update && sudo apt-get install frr frr-pythontools
          sudo systemctl start frr
      # Set up Poetry
      - name: Set up Poetry
        run: |
          sudo apt-get --no-install-recommends --yes install python3-poetry
          poetry install
      # Print IP diagnostics
      - name: Print IP diagnostics
        run: |
          sudo ip address show
          echo '---'
          sudo ip route show
          echo '---'
          sudo vtysh -c 'show ip route'
      # Start the daemon
      - continue-on-error: true
        name: Start ecmp-manager daemon
        run: |
          sudo setcap CAP_NET_RAW=eip $(readlink -f $(poetry run which python3))
          timeout --kill-after=30s --preserve-status 30s poetry run ecmp-manager
      # Print the FRR routing table
      - name: Print the FRR routing table
        run: |
          sudo vtysh -c 'show ip route'
